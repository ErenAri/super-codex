# v2.0.0-beta.2 Release Notes

Date: 2026-03-06
Channel: `next`
Source fragments: 6

## Features

- Made telemetry anonymous opt-in by default with explicit enable/disable controls. (`SC2-007`)
  - Details: Added telemetry status/enable/disable commands and a local JSON export flow for event inspection.
  - Commands: `supercodex growth telemetry status --json`, `supercodex growth telemetry enable --json`, `supercodex growth export --output growth/telemetry-events.json --json`, `supercodex growth telemetry disable --json`
  - References: SC2-007
- Added reusable project presets for init workflow. (`SC2-008`)
  - Details: Supports api-service, web-app, library, and monorepo with additive config merge behavior.
  - Commands: `supercodex init --list-presets`, `supercodex init --preset api-service --refresh-lock`
  - References: SC2-008
- Added release-train workflow for dispatch-driven canary and stable tag orchestration. (`SC2-009`)
  - Details: Release notes are now generated from structured changelog fragments before release tagging.
  - Commands: `npm run release:notes -- --version 2.0.0-beta.2`, `npm run release:notes:check -- --version 2.0.0-beta.2`
  - References: SC2-009
- Added a stable MCP connector contract with official core connectors and capability discovery. (`SC2-020`)
  - Details: Introduced connector registry metadata, connector health diagnostics, and transport-aware capability listing.
  - Commands: `supercodex mcp connectors --official --json`, `supercodex mcp connectors --official --health --connectivity --json`, `supercodex mcp capabilities --official --transport stdio --json`, `supercodex mcp connector docs-retrieval --json`
  - References: SC2-020

## Fixes

- Hardened Windows-sensitive prompt cleanup paths with shared filesystem retry/backoff logic. (`SC2-012`)
  - Details: Prompt uninstall and wrapper cleanup now retry transient EBUSY/ENOTEMPTY/EPERM style failures before failing.
  - Commands: `supercodex uninstall`, `supercodex install`
  - References: SC2-012

## Documentation

- Added SuperClaude parity matrix and migration guide docs for SuperCodex adoption. (`SC2-011`)
  - Details: Includes command syntax guidance for /prompts:* versus CLI alias forms and concrete migration examples.
  - Commands: `supercodex guide "security review" --context chat`, `supercodex /supercodex:research "map migration risks"`
  - References: SC2-011

## Automated Checklist

- [x] Build passes (`npm run build`)
- [x] Tests pass (`npm test`)
- [x] Strict verification passes (`npm run verify:consistency`)
- [x] Release notes generated from structured fragments
- [ ] Publish and verify npm dist-tag (`next` or `latest`)

## Quick Install

```bash
npm install -g supercodex@2.0.0-beta.2
```

